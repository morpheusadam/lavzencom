# Composer dependencies — regenerated via `composer install` on deploy
*/vendor/
vendor/

# Node / build tooling
node_modules/
package-lock.json
yarn.lock

# OS / editor cruft
.DS_Store
Thumbs.db
ehthumbs.db
*.swp
*.swo
*~
.idea/
.vscode/

# Logs / database dumps
*.log
*.sql
*.sql.gz

# WordPress theme local file-editor backups (never commit)
*/.backups/
.backups/

# Build artifacts
dist-zip/
*.zip
